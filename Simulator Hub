if game.PlaceId == 7009799230 then

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
    
    local Window = Library.CreateLib("Pressure Wash Simulator", "Serpent")
    
    local MainTab = Window:NewTab("Main")
        
    local MainSection = MainTab:NewSection("Main")
    
    local AutoFarmTab = Window:NewTab("Auto Farm")
    
    local AutoFarmSection = AutoFarmTab:NewSection("Auto Farming")
    
    local MiscTab = Window:NewTab("Misc")
    
    local MiscSection = MiscTab:NewSection("Misc")
    
    AutoFarmSection:NewToggle("Auto Clean", "Automatically Cleans (make sure to be in your area)", function(state)
        if state then
            getgenv().automoney = true
            while automoney do
                local number_1 = 13283061981201;
                local number_2 = 0;
                local Target = game:GetService("ReplicatedStorage").Remotes.SurfaceCompleted;
                Target:FireServer(number_1, number_2);
    
                local number_1 = 12995553016663;
                local number_2 = 0;
                local Target = game:GetService("ReplicatedStorage").Remotes.SurfaceCompleted;
                Target:FireServer(number_1, number_2);
                
                local number_1 = 12995553016663;
                local number_2 = 0;
                local Target = game:GetService("ReplicatedStorage").Remotes.SurfaceCompleted;
                Target:FireServer(number_1, number_2);
                
                wait()
            end
                else
                    getgenv().automoney = false
        end
    end)
    
    AutoFarmSection:NewToggle("Auto Rebirth", "Automatically Rebirths", function(state)
        if state then
            getgenv().AutoRebirth = true
            while AutoRebirth do
                local Target = game:GetService("ReplicatedStorage").Remotes.RebirthSignal;
                Target:FireServer();
                wait()
            end
            else
                getgenv().AutoRebirth = false
        end
    end)
    
    MiscSection:NewButton("Infinite Money", "Collect Infinite Money", function()
                local number_1 = 12995553016663;
                local number_2 = math.huge;
                local Target = game:GetService("ReplicatedStorage").Remotes.SurfaceCompleted;
                Target:FireServer(number_1, number_2);
    end)
    
    MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
        Library:ToggleUI()
    end)
    
    MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
        setclipboard("https://discord.gg/D9xuHyRcWK")
    end)
    
elseif game.PlaceId == 2580982329 then
    
    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
    
    local Window = Library.CreateLib("Texting Simulator", "Serpent")
    
    local MainTab = Window:NewTab("Main")
        
    local MainSection = MainTab:NewSection("Main")
    
    local AutoTab = Window:NewTab("Auto Farm")
    
    local AutoSection = AutoTab:NewSection("Auto Farming")
    
    AutoSection:NewToggle("Auto Text", "Auto Send Texts", function(state)
        if state then
            
            local phonething = "Phone";
            local tabletthing = "Tablet";
            local ppeater = game:GetService("ReplicatedStorage").Events.SendTexts;
            local string_1 = "Computer"; 
            local Target = game:GetService("ReplicatedStorage").Events.SendTexts;
            getgenv().AutoFarm1 = true
            while AutoFarm1 do
                Target:FireServer(tabletthing);
                ppeater:FireServer(phonething);
                Target:FireServer(string_1);
                Target:FireServer("Game");
                wait()
            end
        else
            getgenv().AutoFarm1 = false
        end
    end)
    
    MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
        Library:ToggleUI()
    end)
    
    MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
        setclipboard("https://discord.gg/D9xuHyRcWK")
    end)

elseif game.PlaceId == 4822852675 then

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

    local Window = Library.CreateLib("☀️Typing Simulator 📱", "Serpent")

    local MainTab = Window:NewTab("Main")

    local MainSection = MainTab:NewSection("Main")

    local AutoFarmTab = Window:NewTab("Auto Farm")

    local AutoFarmSection = AutoFarmTab:NewSection("Auto Farm")

    AutoFarmSection:NewToggle("Auto Type", "Automatically Farms Messages", function(state)
        if state then
            getgenv().AutoMessage = true
            while AutoMessage do
                local string_1 = "Click";
                local Target = game:GetService("ReplicatedStorage").Remotes.Mines["2245036054"];
                Target:FireServer(string_1);
                local string_1 = "Worker";
                local Target = game:GetService("ReplicatedStorage").Remotes.Mines["2245036054"];
                Target:FireServer(string_1);
                Target:FireServer(string_1);
                Target:FireServer(string_1);
                wait()
            end
            else
                getgenv().AutoMessage = false
        end
    end)

    MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
        Library:ToggleUI()
    end)

    MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
        setclipboard("https://discord.gg/D9xuHyRcWK")
    end)

elseif game.PlaceId == 3101667897 then

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

    local Window = Library.CreateLib("Legends Of Speed ⚡", "Serpent")

    local MainTab = Window:NewTab("Main")

    local MainSection = MainTab:NewSection("Main")

    local AutoFarmTab = Window:NewTab("Auto Farm")

    local AutoFarmSection = AutoFarmTab:NewSection("Auto Farm")

    MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
        Library:ToggleUI()
    end)

    MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
        setclipboard("https://discord.gg/D9xuHyRcWK")
    end)

    AutoFarmSection:NewToggle("Auto Steps", "Automatically Farms Steps", function(state)
        if state then
            getgenv().AutoFarm = true
            while AutoFarm do
                local string_1 = "collectOrb";
                local string_2 = "Red Orb";
                local string_3 = "City";
                local Target = game:GetService("ReplicatedStorage").rEvents.orbEvent;
                Target:FireServer(string_1, string_2, string_3);
                wait()
            end
            else
                getgenv().AutoFarm = false
        end
    end)

    AutoFarmSection:NewToggle("Auto Rebirth", "Automatically Rebirths when possible.", function(state)
        if state then
            getgenv().AutoRebirth = true
            while AutoRebirth do
                local string_1 = "rebirthRequest";
                local Target = game:GetService("ReplicatedStorage").rEvents.rebirthEvent;
                Target:FireServer(string_1);
                wait()
            end
            else
                getgenv().AutoRebirth = false
        end
    end)

elseif game.PlaceId == 1828509885 then

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

    local Window = Library.CreateLib("Egg Farm Simulator", "Serpent")

    local MainTab = Window:NewTab("Main")
        
    local MainSection = MainTab:NewSection("Main")
    
    local AutoFarmTab = Window:NewTab("Auto Farm")
    
    local AutoFarmsection = AutoFarmTab:NewSection("Auto Farm")
    
    AutoFarmsection:NewToggle("Auto Kill Chickens", "Automatically Hits Chickens", function(state)
        if state then
            getgenv().AutoSwing = true
            while AutoSwing do
                game:GetService("ReplicatedStorage").Events.Weapon:FireServer("self", "219629d5067eddcdce55ed7968e9b53f")
                wait()
            end
            else
                getgenv().AutoSwing = false
        end
    end)

    MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
        Library:ToggleUI()
    end)

    MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
        setclipboard("https://discord.gg/D9xuHyRcWK")
    end)

elseif game.PlaceId == 3652625463 then

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

    local Window = Library.CreateLib("💪🏻 Lifting Simulator", "Serpent")

    local MainTab = Window:NewTab("Main")

    local MainSection = MainTab:NewSection("Main")

    local AutoFarmTab = Window:NewTab("Auto Farm")

    local AutoFarmSection = AutoFarmTab:NewSection("Auto Farm")

    AutoFarmSection:NewToggle("Auto Punch", "Automatically farms punches", function(state)
        if state then
            getgenv().AutoPunch = true
            while AutoPunch do
                local table_1 = {
                	[1] = 'GainMuscle'
                };
                local Target = game:GetService("ReplicatedStorage").RemoteEvent;
                Target:FireServer(table_1);

                wait()
            end
                else
        getgenv().AutoPunch = false
        end
    end)

    AutoFarmSection:NewToggle("Auto Sell", "Automatically sells punches", function(state)
        if state then
            getgenv().AutoSell = true
            while AutoSell do

                local table_1 = {
                	[1] = 'SellMuscle'
                };
                local Target = game:GetService("ReplicatedStorage").RemoteEvent;
                Target:FireServer(table_1);

                wait()
            end
            else
                getgenv().AutoSell = false
        end
    end)

    MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
        Library:ToggleUI()
    end)

    MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
        setclipboard("https://discord.gg/D9xuHyRcWK")
    end)

elseif game.PlaceId == 2619187362 then

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

local Window = Library.CreateLib("⚡ Super Power Fighting Simulator", "Serpent")

local MainTab = Window:NewTab("Main")
    
local MainSection = MainTab:NewSection("Main")

local AutoFarmTab = Window:NewTab("Auto Farm")

local AutoFarmSection = AutoFarmTab:NewSection("Auto Farm")

AutoFarmSection:NewToggle("Auto Strength", "Automatically farms Strength", function(state)
    if state then
        getgenv().AutoStrength = true
        while AutoStrength do
            local string_1 = "Strength";
            local Target = game:GetService("ReplicatedStorage").Events.Train;
            Target:FireServer(string_1);
            wait()
        end
    else
        getgenv().AutoStrength = false
    end
end)

AutoFarmSection:NewToggle("Auto Endurance", "Automatically farms Endurance", function(state)
    if state then
        getgenv().AutoEndurance = true
        while AutoEndurance do
            local string_1 = "Endurance";
            local Target = game:GetService("ReplicatedStorage").Events.Train;
            Target:FireServer(string_1);
            wait()
        end
    else
        getgenv().AutoEndurance = false
    end
end)

AutoFarmSection:NewToggle("Auto Psychic", "Automatically farms Psychic", function(state)
    if state then
        getgenv().AutoPsy = true
        while AutoPsy do
            local string_1 = "Psychic";
            local Target = game:GetService("ReplicatedStorage").Events.Train;
            Target:FireServer(string_1);
            wait()
        end
    else
        getgenv().AutoPsy = false
    end
end)


MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
    Library:ToggleUI()
end)

MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
    setclipboard("https://discord.gg/D9xuHyRcWK")
end)

elseif game.PlaceId == 6440133276 then

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

local Window = Library.CreateLib("Shampoo Simulator", "Serpent")

local MainTab = Window:NewTab("Main")
    
local MainSection = MainTab:NewSection("Main")

local AutoFarmtab = Window:NewTab("Auto Farm")

local AutoFarmSection = AutoFarmtab:NewSection("Auto Farm")

AutoFarmSection:NewToggle("Auto Shampoo", "Automatically farms Shampoo", function(state)
    if state then
        getgenv().AutoShampoo = true
        while AutoShampoo do
            local Target = game:GetService("Players")[""..game.Players.LocalPlayer.Name..""].Backpack.Shampoo.Remotes.Events.RequestPoints;
            Target:FireServer();
            wait()
        end
    else
        getgenv().AutoShampoo = false
    end
end)

MainSection:NewKeybind("Toggle UI Keybind", "Add a Toggle UI Keybind", Enum.KeyCode.RightControl, function()
    Library:ToggleUI()
end)

MainSection:NewButton("Copy Discord Invite", "https://discord.gg/D9xuHyRcWK", function()
    setclipboard("https://discord.gg/D9xuHyRcWK")
end)

end
